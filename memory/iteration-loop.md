---
name: iteration-loop
description: The fast hcode iteration method lives in HCODE.md → "Iteration loop"; read it before any UI/CLI change
type: reference
---

The method (local branch → `hcode demo` → `npm test` → one commit per topic → one-line report;
three render paths; tests describe the shape; no mid-stream version bump) is written in
`HCODE.md` under "Iteration loop". It was set on 2026-08-30 after an afternoon that shipped
twelve commits this way.

**Why:** the owner wants the same speed on every machine and from every agent, and does not
want the method stored in any one agent's private memory — it belongs to the code.

**How to apply:** do not copy the method here; read that section, follow it, and if you improve
the method, edit that section (public-safe) and add a memory only for the non-obvious lesson.
